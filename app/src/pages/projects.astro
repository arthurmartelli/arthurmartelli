---
import PageLayout from "~/layouts/PageLayout.astro";
import PageHeader from "~/components/ui/PageHeader.astro";
import CardGrid from "~/components/sections/CardGrid.astro";
import { SOCIALS } from "~/consts";
import {
    fetchUserRepositories,
    filterNonForks,
    sortByStars,
    type GitHubRepo,
} from "~/utils/github";

let repos: GitHubRepo[] = [];

try {
    const allRepos = await fetchUserRepositories(SOCIALS.github.username);
    repos = sortByStars(filterNonForks(allRepos));
} catch (error) {
    console.error("Failed to fetch GitHub repos:", error);
}
---

<PageLayout
    title="Projects"
    description="My GitHub projects and repositories"
    maxWidth="wide"
>
    <PageHeader
        title="My Projects"
        subtitle="Open source repositories and personal projects"
    />

    {
        repos.length > 0 ? (
            <CardGrid>
                {repos.map((repo) => (
                    <a
                        href={repo.html_url}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="project-card"
                    >
                        <div class="project-header">
                            <h3>{repo.name}</h3>
                        </div>

                        <div class="project-stats">
                            <span class="stat">⭐ {repo.stargazers_count}</span>
                            <span class="stat">🔀 {repo.forks_count}</span>
                        </div>

                        <p class="project-description">
                            {repo.description || "No description available"}
                        </p>

                        <div class="project-footer">
                            {repo.language && (
                                <div class="language">
                                    <span class="language-dot" />
                                    {repo.language}
                                </div>
                            )}
                            <span class="updated">
                                Updated{" "}
                                {new Date(repo.updated_at).toLocaleDateString()}
                            </span>
                        </div>
                    </a>
                ))}
            </CardGrid>
        ) : (
            <div class="no-repos">
                <p>No repositories found or failed to load.</p>
            </div>
        )
    }
</PageLayout>

<style>
    .project-card {
        background: rgb(var(--gray-light), 0.3);
        border: 1px solid rgb(var(--gray-light));
        border-radius: 12px;
        padding: 1.5rem;
        display: flex;
        flex-direction: column;
        transition: all 0.3s ease;
        text-decoration: none;
        color: inherit;

        &:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            border-color: var(--accent);
        }

        & h3 {
            margin: 0;
            font-size: 1.3rem;
            color: var(--accent);
        }

        @media (max-width: 640px) {
            padding: 1.25rem;
        }
    }

    .project-header {
        margin-bottom: 1rem;
    }

    .project-stats {
        display: flex;
        gap: 1rem;
        font-size: 0.9rem;
        color: rgb(var(--gray));
        margin-bottom: 1rem;

        @media (max-width: 640px) {
            flex-wrap: wrap;
        }
    }

    .stat {
        display: flex;
        align-items: center;
        gap: 0.3rem;
    }

    .project-description {
        color: rgb(var(--gray-dark));
        flex: 1;
        margin-bottom: 1rem;
        line-height: 1.6;
    }

    .project-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 1rem;
        border-top: 1px solid rgb(var(--gray-light));

        @media (max-width: 640px) {
            flex-direction: column;
            gap: 0.5rem;
            align-items: flex-start;
        }
    }

    .language {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.9rem;
        color: rgb(var(--gray-dark));
    }

    .language-dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
        background: var(--accent);
    }

    .updated {
        font-size: 0.85rem;
        color: rgb(var(--gray));
    }

    .no-repos {
        text-align: center;
        padding: 3rem 1rem;
        color: rgb(var(--gray));
    }
</style>
