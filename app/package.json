{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --host ${APP_HOST} --port ${APP_PORT}",
    "build": "astro build",
    "preview": "astro preview --host ${APP_HOST} --port ${APP_PORT}",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.13",
    "@astrojs/rss": "^4.0.14",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.16.6",
    "sharp": "^0.34.3"
  }
}
